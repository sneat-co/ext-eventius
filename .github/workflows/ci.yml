name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  contract_boundary:
    name: contract capability boundary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject implementation and host dependencies
        shell: bash
        run: |
          set -euo pipefail
          violations="$(git grep -nE '"github\.com/(sneat-co/(sneat-go|sneat-bots|[^" ]+/backend)|dal-go/)' -- ':(glob)backend/**/*.go' || true)"
          violations="$(printf '%s\n' "$violations" | grep -v '"github\.com/sneat-co/ext-eventius/backend/' || true)"
          if [ -n "$violations" ]; then
            printf '%s\n' "$violations" >&2
            echo 'ext-eventius is a contract module and may not import implementations, hosts, or DAL packages.' >&2
            exit 1
          fi

  backend:
    name: backend build + test
    needs: contract_boundary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: backend/go.mod
      - name: Build
        working-directory: backend
        run: go build ./...
      - name: Test
        working-directory: backend
        shell: bash
        run: |
          set -euo pipefail
          go test ./... -coverprofile=coverage.out
          coverage="$(go tool cover -func=coverage.out | awk '/^total:/ {print $3}' | tr -d '%')"
          floor="$(tr -d '[:space:]' < coverage_floor.txt)"
          awk -v coverage="$coverage" -v floor="$floor" 'BEGIN { exit !(coverage >= floor) }' || {
            echo "coverage ${coverage}% is below floor ${floor}%" >&2
            exit 1
          }
