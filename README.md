# ext-eventius

Public contracts for the Eventius extension. The compiled npm package
`@sneat/extension-eventius-contract` contains its DTOs, service interfaces, and
DI tokens; Eventius implementation remains private in `sneat-co/eventius`.
